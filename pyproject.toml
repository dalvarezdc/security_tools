[tool.poetry]
name = "poetry-security-tools"
version = "0.1.0"
description = ""
authors = ["Dan"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
dexscreener = "^1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
